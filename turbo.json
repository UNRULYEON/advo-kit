{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "lint": {},
    "dev": {
      "cache": false,
      "persistent": true
    },
    "migrations:generate": {
      "cache": false
    },
    "migrations:push": {
      "cache": false
    },
    "migrations:up": {
      "cache": false
    }
  }
}
